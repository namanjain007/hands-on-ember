import Controller from '@ember/controller';

export default class CartController extends Controller {//anythig defined in this class can be used in template
    cartName = "MyFirstCart";
    cartSubTotal = 0;
}
